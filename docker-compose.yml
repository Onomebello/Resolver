services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: resolver
      POSTGRES_PASSWORD: resolver
      POSTGRES_DB: resolver
    ports:
      - "5432:5432"
    volumes:
      - resolver-postgres-data:/var/lib/postgresql/data

volumes:
  resolver-postgres-data:
